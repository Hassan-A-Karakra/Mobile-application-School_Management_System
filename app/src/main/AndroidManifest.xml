<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SchoolManagementSystem"
        tools:targetApi="31">
        <activity
            android:name=".CommunicateActivity"
            android:exported="false" />
        <activity
            android:name=".ReportsActivity"
            android:exported="true" /> <!-- تغيير إلى true إذا كنت بحاجة للوصول إليها باستخدام Intent -->
        <activity
            android:name=".AttendanceActivity"
            android:exported="true" /> <!-- تغيير إلى true إذا كنت بحاجة للوصول إليها باستخدام Intent -->
        <activity
            android:name=".GradeInputActivity"
            android:exported="false" />
        <activity
            android:name=".StudentListActivity"
            android:exported="false" />
        <activity
            android:name=".StudentActivity"
            android:exported="false" />
        <activity
            android:name=".TeacherActivity"
            android:exported="true" /> <!-- تأكد من أن هذا "true" للوصول إلى الشاشة من "LoginActivity" -->
        <activity
            android:name=".RegistrarActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- إزالة intent-filter من LoginActivity لأنك لا تريد أن تظهر أولًا -->
        <activity
            android:name=".LoginActivity"
            android:exported="true" />
    </application>

</manifest>